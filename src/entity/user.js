export class User {
  id;
  name;
  email;
  password;
  address;

  constructor(id, name, email, password, address) {
    this.id = id;
    this.name = name;
    this.email = email;
    this.password = password;
    this.address = address;
  }

  // Implementações de lógicas de negócio...
}